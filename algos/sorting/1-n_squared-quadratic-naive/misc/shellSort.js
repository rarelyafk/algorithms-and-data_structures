#!/usr/bin/env node

// variation of insertion sort
/*
 *
 */

const shellSort = (arr) => {
  return arr;
};

const arr = [8,2,4,1,3];
console.log(`before: ${arr}`);
const res = shellSort(arr);
console.log(` after: ${res}`)

/*

*/
